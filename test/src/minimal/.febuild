const path = require('path');

module.exports =  {
    babel: {
        use: {
            options: {
            presets: [['@babel/preset-env', { useBuiltIns: 'usage', corejs: 3, debug: true }]]
            }
        }
    },
  };
